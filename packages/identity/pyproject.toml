[project]
name = "eaos-identity"
version = "0.1.0"
description = "EAOS Bounded Context Domain Package: identity"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.28"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packages/identity"]