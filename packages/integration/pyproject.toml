[project]
name = "eaos-integration"
version = "0.1.0"
description = "EAOS Bounded Context Domain Package: integration"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling>=1.28"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packages/integration"]