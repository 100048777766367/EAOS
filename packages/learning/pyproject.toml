[project]
name = "eaos-learning"
version = "0.1.0"
description = "EAOS Learning Engine Package"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["domain", "application", "infrastructure"]