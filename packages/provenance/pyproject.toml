[project]
name = "eaos-provenance"
version = "0.1.0"
description = "EAOS Enterprise Provenance Package"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"
